apply plugin: "application"

dependencies {
    implementation project(":core")
}

application {
    mainClass = "com.battlecity.server.HeadlessServer"
}

tasks.named("run", JavaExec).configure {
    standardInput = System.in
}
