allprojects {
  apply plugin: "idea"

  repositories {
    mavenCentral()
  }
}

subprojects {
  apply plugin: "java"

  tasks.withType(JavaCompile).configureEach {
    options.encoding = "UTF-8"
    options.release = 17
  }
}

